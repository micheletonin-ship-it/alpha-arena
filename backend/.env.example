# Backend Environment Variables (Alpha Arena)
# Copy this file to .env and fill in your values

# Server Configuration
NODE_ENV=development
PORT=3001

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Supabase Configuration (Service Role Key for admin operations)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Stripe Configuration (Optional defaults - users store their own keys in DB)
# Only needed if you want server-side defaults
STRIPE_SECRET_KEY=sk_test_your_test_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Note: In production on Railway, set these environment variables in the Railway dashboard
